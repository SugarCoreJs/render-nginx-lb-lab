# default.conf.template
upstream backend_servers {
    server ${BACKEND1}:80;
    server ${BACKEND2}:80;
}

server {
    listen 80;

    location / {
        proxy_pass http://backend_servers;
    }
}